package org.example.access.id.user;

import org.example.access.id.ID;
import org.example.access.validate.ID_Validator;

public class UserID implements ID {

    ID_Validator idValidator = new ID_Validator();
}
